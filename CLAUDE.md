# 家計簿スキャナー — 開発ガイド

## 目次
- [構成概要](#構成概要)
- [タブ構成](#タブ構成)
- [データ構造](#データ構造)
- [GAS API](#gas-api)
- [主要関数](#主要関数)
- [資産の考え方](#資産の考え方)
- [サービス内容の定義](#サービス内容の定義)
- [課題・TODO](#課題todo)
- [開発ノート](#開発ノート)

---

## 構成概要

| ファイル | 役割 |
|---------|------|
| `index.html` | フロントエンド全体（HTML + CSS + JS 単一ファイル、約3200行） |
| `gas-code.gs` | バックエンド（Google Apps Script）|
| `manifest.json` | PWA設定 |
| `icon.svg` | アプリアイコン |
| `tasks.html` | 開発タスク管理（カンバン、localStorage保存） |

フロントは**単一HTMLファイルのSPA**。バックエンドはGoogle SheetsをDBとして使うGAS。

**index.html を編集するときの注意:**
- ファイルが大きいので**必ずoffset+limitで対象セクションを部分読み込み**すること
- CSSはファイル冒頭付近（〜340行）、HTMLは340〜1060行、JSは1060行以降
- タブのHTML構造はそれぞれ `id="page-*"` のdivで分かれている

---

## タブ構成

| タブ | id | 主な機能 |
|-----|----|---------|
| ✏️ 手入力 | `page-input` | サービス選択 → 金額・品目・支払入力 → 登録 |
| 📷 スキャン | `page-scan` | レシート写真 → Claude AIで自動入力 |
| 📊 ダッシュボード | `page-dashboard` | 月次集計・グラフ・クレジット明細 |
| 💰 資産 | `page-assets` | 資産別残高・推移グラフ・在庫 |
| 📋 一覧 | `page-list` | 全件表示・編集・削除 |
| ⚙️ 設定 | `page-settings` | GAS URL・Claude APIキー・資産初期値 |

---

## データ構造

**Google Sheets「全データ」シート：**

| 列 | カラム名 | 内容 |
|----|---------|------|
| 1 | 取引ID | 追加時のlastRow値 |
| 2 | サービスID | 関連レコードをグループ化するID |
| 3 | 日付 | `yyyy-MM-dd` |
| 4 | サービス内容 | 購入 / チャージ / キャッシュバック など |
| 5 | 品目1 | カテゴリ1 |
| 6 | 品目2 | カテゴリ2 |
| 7 | 店 | 店舗名 |
| 8 | 金額 | 数値 |
| 9 | 支払い手段1 | 現金 / クレカ / 電子マネー など |
| 10 | 支払い手段2 | サブ支払い手段（チャージ元など） |
| 11 | 記録日時 | タイムスタンプ |
| 12 | 数量 | 在庫管理用（準資産のみ） |

**serviceId の役割:** 一つの「取引イベント」に紐づく複数レコードを同じIDで束ねる（例: ギフト券購入レコードと資産追加レコードを同一IDにする）。

**localStorage キー:**

| キー | 内容 |
|-----|------|
| `claudeKey` | Claude API キー（スキャン機能用） |
| `kakeiboGasUrl` | GASデプロイURL |
| `assetInits` | 資産別初期残高（JSON: `{ "現金": 10000, ... }`） |
| `kakeibo_lastServiceId` | 最後に使ったserviceId（採番継続用） |

---

## GAS API

**エンドポイント:** 設定タブで入力したGASデプロイURL（`kakeiboGasUrl`）

| メソッド | action | 説明 |
|---------|--------|------|
| GET | `add` | 1件追加 |
| GET | `list` | 全件取得 |
| GET | `info` | スプレッドシート情報 |
| GET | `delete` | 1件削除（`id`パラメータ） |
| GET | `update` | 1件更新（`id`パラメータ） |
| GET | `updateInventoryItem` | 準資産サマリーのupsert（no-cors） |
| GET | `getInventorySummary` | 準資産一覧取得 |
| POST | `batchAdd` | 複数件一括追加 |

書き込み系はno-corsモード（fire & forget）。`list`・`info`・`getInventorySummary`はレスポンスを読む。

**外部API:**
- **Claude API** (`claude-haiku-4-5`): レシートスキャン。`scanReceipt()` / `scanAssetStatement()` から呼ぶ。直接ブラウザから叩く（`anthropic-dangerous-direct-browser-access: true`）。

---

## 主要関数

| 関数 | 役割 |
|------|------|
| `registerRecord()` | 手入力フォームの登録エントリーポイント |
| `doRegister(record)` | GASへ実際にPOSTする処理 |
| `scanReceipt()` | スキャンタブのAI解析フロー |
| `loadAllData()` | 起動時・操作後のGAS全件取得 |
| `renderDashboard()` | ダッシュボード描画 |
| `renderAssets()` | 資産一覧描画 |
| `renderList()` | 一覧タブ描画 |
| `batchAddRecords()` | スキャン結果の一括登録 |
| `calcTotalAssetsAt()` | 指定日時点の総資産を計算 |
| `getInventoryFromRecords()` | レコードから在庫を集計 |

---

## 資産の考え方

### 基準と分類

- **基準日:** 2025/08/23 — この日に全保有資産を棚卸し・初期値を設定した
- **本資産:** `ASSETS` 配列に定義された資産。`assetInits` に初期残高を持ち、取引差分で現在残高を算出
- **準資産:** ギフト券・物品など。`資産追加`レコードで管理。amount=点数、想定価値をGAS `準資産サマリー` シートで保持

### 本資産への支払い手段追加方法

新しい支払い手段を追加するときに変更が必要な箇所（`index.html`）：

| 箇所 | 行あたり | 内容 |
|------|---------|------|
| `ASSETS` 配列 | ~L1547 | `{ key, label, type }` を追加（銀行/QR決済/プリペイド/ポイント/現金） |
| `EMONEY_MAP` | ~L1106 | typeに対応するグループへ追加 |
| `VALID_PAYMENT` | ~L1712 | マスターリストへ追加 |
| `PAYMENT_NORMALIZE` | ~L1713 | 表記ゆれがあれば追加 |
| AIプロンプト | ~L1652 | payment1選択肢リストへ追加 |
| `r-payment1` select | ~L706 | スキャンタブのドロップダウンへ追加 |
| `SQ_PAYMENT_LIST` | ~L1988 | クイック登録の支払いチップへ追加 |
| `ECO_COLORS` | ~L3706 | 生態系ビューの色マップへ追加 |

### 将来展望：動的な資産管理

現状、資産・支払い手段はすべてコードにハードコードされている。将来的には：
- カスタム資産を `localStorage`（軽量・即実装）または GASシート（端末間共有）に保存
- 起動時にハードコード配列とマージして動的に使用
- 設定タブに「資産を追加」UIを設ける（資産名・type・初期残高を入力）
- 追加した資産が即座に支払い手段チップ・ドロップダウン・グラフに反映

### ギフト券・物品の扱い

- ネットで購入 → `購入（ギフト券類）`として支出記録 → 同じserviceIdで`資産追加`レコードが自動生成
- 資産追加レコードの amount = 点数（円でなく）
- 想定価値（円/点）はGAS `準資産サマリー` シートで管理
- 転売・換金時は逆方向で記録（TODO: フローを確定させる）

---

## サービス内容の定義

| サービス内容 | 概要 |
|------------|------|
| 購入 | 通常の買い物 |
| 立て替え | 他者分の代払い |
| もらう / あげる | 人とのお金のやり取り |
| キャッシュバック | ポイント・キャッシュバックを受け取るとき |
| チャージ | 電子マネー・プリペイドへの入金 |
| ATM入金 / ATM出金 | ATMでの現金操作 |
| 振込入金 / 振込出金 | 銀行振込 |
| 給与 | 給与受け取り |
| 売却 | 資産の売却 |
| クレカ引き落とし | クレカ利用額の引き落とし |
| 資産追加 | 準資産（ギフト券等）を在庫に加えるとき（注意: serviceIdで購入レコードと必ず紐づける） |

---

## 課題・TODO

- `index.html` のコード整理（単一ファイルSPAが約3200行に肥大化）
- 転売・換金フローの確定（ギフト券売却時の記録方法）
- サービス内容（購入・立て替えなど）の説明をCLAUDE.mdに追記
- 資産の動的な管理（設定タブに「資産を追加」UIを実装し、資産名・type・初期残高を入力できるようにする）= 現状どんな資産があるのか調査してレポートにしてください = kakeiboの根幹を成します。これからの開発で最も重要で今行き詰まっている所です。論理的に正しい実装方向を目指すために現状をレポートにしてください。包括的にお願いします。
---

## 開発ノート

### 2026-06-16: 複雑なレシート（複数カテゴリ混在）の扱いを検討

**課題:** 1枚のレシートに品目1（カテゴリ）が異なる商品が混在している場合、現状のスキャン機能は1レシート=1レコード（品目1/2はそれぞれ1つ）しか生成できないため、ユーザーが手動で複数レコードに分けて登録する必要がある。

**検討した案:**
- AIプロンプトを「品目ごとの配列」を返す形式に変え、資産スキャン（`asset-scan-results`）と同様の複数行編集UIで一括登録（`batchAdd`）する方法を検討したが、見送り。

**方針決定:**
- このアプリの根幹は「正確な分類による記録」であり、AIによる品目自動分割は誤分類リスクとそのチェック・修正コストが見合わないと判断。
- 複数カテゴリが混在する複雑なレシートは、多少手間でも**手入力タブで品目ごとに分けて登録**する運用とする。
- スキャン機能自体（1レシート=1レコード）は変更しない。

**次のアクション（実装は本日中に着手予定）:**
- 手入力タブでの分割登録を楽にするための具体案を、別途このノートに追記して実装する（例: 直前入力の日付・店・支払い手段の引き継しなど）。

---

### 2026-06-16: 複数カテゴリーレシートの「税込み額計算」強化（提案）

**課題（端数調整の手間）:**
- カテゴリーごとに税率が揃っていれば分類自体は問題ない。問題は、レシートには税抜き額で品目が書かれていることが多く、税込み額に変換する際の端数処理（切り捨て/四捨五入/切り上げ）次第で、各カテゴリーの税込み額の合計が「レシート記載の合計金額」と1円前後ズレること。これを毎回手計算で調整しているのが面倒。

**提案フロー:**
1. まず「合計金額（レシート記載の税込み合計）」を入力する。
2. **1カテゴリーのみの場合:** 従来通り、品目を1つ選んで完了（合計金額＝その品目の金額として一発登録、現状のフローと同じ）。
3. **複数カテゴリーの場合:**
   - カテゴリーを1つずつ追加していく。
   - **各カテゴリー（最後も含めて全て）について**: 税抜き額を入力 → 端数処理方法（切り捨て/四捨五入/切り上げ、カテゴリーごとに選択）→ 品目の税率（8%/10%、自動判定）で税込み額を計算。
   - 全カテゴリー入力後、各税込み額の合計と①で入力した「合計金額」を**照合**する（入力ミスのチェックにもなる）。
   - ズレ（だいたい1円程度）が出たら、いずれかのカテゴリー（最後のものが分かりやすい）の端数処理方法を切り捨て⇔四捨五入⇔切り上げで切り替えて、合計が一致するように調整する。
   - 各カテゴリーは同serviceId・同日付・同店舗・同支払い手段で複数レコードとして登録する（取引IDはレコードごとに別々）。

**懸念:** 計算ロジックがやや複雑になる可能性あり。

#### 検討事項・コメント

- **データ構造への影響:** 保存するのは最終的な税込み額（amount）のみで、税率・端数処理方法はUI上の計算補助に過ぎず保存しない方針でよさそう（既存のデータ構造変更は不要）。
- **適用範囲:** この強化は手入力タブのみ。スキャンタブは前回決定どおり変更なし（1レシート=1レコード）。
- **UIイメージ:** カテゴリー行ごとに「税抜き額入力」「端数処理（切り捨て/四捨五入/切り上げのトグル、デフォルトは切り捨てか四捨五入）」「計算結果の税込み額（自動表示）」。全行の下に「税込み額合計」と「合計金額との差分」を常時表示し、差分0で登録ボタンを有効化、差分≠0なら警告色で表示してどの行の端数処理を変えれば良いか分かるようにする。

**決定事項:**
1. **税率は品目1から自動判定**（食料品=8%・他=10%固定、ユーザー選択は不要）。外食は単一カテゴリーで合計金額をそのまま記録する運用のためそもそも複数カテゴリー分割の対象にならず、例外を考慮する必要はない。通常の買い物では品目1で税率が一意に決まる。
2. **端数処理（切り捨て/四捨五入/切り上げ）はカテゴリーごとにユーザーが選択**。同じ端数処理で揃えると合計が1円前後ズレるため、全カテゴリー分の税込み額を計算→合計金額と照合→ズレたらどこか1カテゴリーの端数処理を変えて一致させる、という運用。
3. **複数カテゴリーのレコード群は同じserviceIdで束ねる**。取引ID（各レコードのID）はこれまで通りレコードごとに別々（lastRowベースで自動付与）。

**将来TODO:** クーポン・割引適用時の計算も楽にしたい（今回のスコープ外、別途検討）。

---

### 2026-05-23: 全チップ・セレクトを動的生成に統一

asset.md の枠組み別管理方針をUIに反映。

**変更内容:**
- WAON の type 不整合を解消（BUILTIN_EMONEY_MAP: ポイント → プリペイド）
- 以下6セクションのハードコードチップを削除し、`buildDynamicUI()` で ASSETS から動的生成:
  - 受け取り先 `f-uketori-chips`（キャッシュバック・売却 → 全資産）
  - 授受先 `f-jusho-chips`（もらう・あげる → 全資産）
  - 給与受取口座 `f-kyuyo-chips`（銀行型資産のみ）
  - 銀行 `f-bank-chips`（ATM・振込・クレカ引き落とし → 銀行型資産のみ）
  - 振込先 `f-furikomi-to-chips`（銀行型資産のみ）
  - チャージ元 `f-charge-from-chips`（クレカ + 全資産）
- 既存の チャージ先・スキャンタブセレクトも含め、全入力欄が資産追加に追従するように

**効果:** カスタム資産を追加した際、手入力タブ・スキャンタブの全ての選択肢に自動で反映される。

---

### 2026-05-23: 資産の動的管理を実装

`ASSETS` 配列を唯一のsource of truth にするリファクタリング。

**主な変更:**
- `ASSETS` → `BUILTIN_ASSETS`、`EMONEY_MAP` → `BUILTIN_EMONEY_MAP` に rename
- 起動時に `localStorage.customAssets` とマージして `ASSETS` を動的生成
- `VALID_PAYMENT`・`SQ_PAYMENT_LIST`・`AQ_CHARGE_FROM_LIST`・`ECO_COLORS` をすべて `ASSETS` から自動派生
- チャージ先チップ・スキャンタブの支払いセレクトを `buildDynamicUI()` で動的ビルド
- 設定タブに「カスタム資産」セクション追加（資産名・type・初期残高・色を入力→追加）
- AIプロンプトの payment1 選択肢を `VALID_PAYMENT` から動的生成

**今後の資産追加:** `BUILTIN_ASSETS` の1行追加 + `BUILTIN_ECO_COLORS` の1行追加のみでOK（旧来の8ヶ所変更は不要）。

---

### 2025-05-23: Suica を本資産として追加

type=`プリペイド` として `ASSETS` 配列に追加。変更箇所は全8ヶ所（ASSETS, EMONEY_MAP, VALID_PAYMENT, PAYMENT_NORMALIZE, AIプロンプト, r-payment1 select, SQ_PAYMENT_LIST, ECO_COLORS）。チャージ先としてクイック登録から使用可能。

---

### 2025-05-23: 準資産（ギフト券類）システム刷新

ギフト券類の購入・在庫管理フローを整理。

**主な変更:**
- 購入フォームを簡略化（品目名・数量フィールドを廃止し、資産追加情報を別枠で収集）
- 資産追加レコードの `amount` = 点数（円でなく）に統一
- 想定価値（1点あたりの円換算）をGAS `準資産サマリー` シートで管理
- 資産タブで「点数 × 想定価値」から準資産の金額表示・総資産への加算
- 準資産ごとの点数月次推移グラフを追加

**資産追加レコードのデータモデル（統一後）:**

| フィールド | 値 |
|-----------|---|
| amount | 点数 |
| payment1 | 資産名 |
| quantity | 空（旧データは quantity に点数が入っている場合あり） |
| category1 | ギフト券類 or 空 |

後方互換: `const qty = Number(r.quantity || r.amount) || 0;` で旧データも対応。

**GAS 新シート `準資産サマリー`（1資産1行）:**

| A: 資産名 | B: 点数 | C: 想定価値（円/点） | D: 最終更新 |
